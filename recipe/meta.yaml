{% set goname = "github.com/gruntwork-io/terragrunt" %}
{% set version = "0.23.6" %}

{% set name = goname.split('/')[-1] %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://{{ goname }}/archive/v{{ version }}.tar.gz
  sha256: 0ce75511482c6ab4e49fb4dec5f3c93c79c62bd009606249b7763e270746becc

build:
  number: 0
  script:
    - go install -v -ldflags "-X main.VERSION={{ version }}" .

requirements:
  build:
    - {{ compiler('go') }}

test:
  requires:
    - m2-bash  # [win]
  commands:
    - bash -c "{{ name|lower }} --help"
    - bash -c "{{ name|lower }} --version"

about:
  home: https://www.gruntwork.io/
  license: MIT
  license_file: LICENSE.txt
  summary: Terragrunt is a thin wrapper for Terraform that provides extra tools for working with multiple Terraform modules.
  dev_url: https://github.com/gruntwork-io/terragrunt.git

extra:
  recipe-maintainers:
    - sodre
    - mariusvniekerk
